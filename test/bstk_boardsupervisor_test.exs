defmodule BstkSupervisorTest do
  use ExUnit.Case
  alias Bstk.{BoardServer, BoardSupervisor}


  #test "Spin up new process from process_id" do
  #  BoardSupervisor.start_link()
  #  {:ok, _game} = BoardSupervisor.find_or_create_process(26)
  #end

end
